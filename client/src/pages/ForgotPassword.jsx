import { useState } from 'react'; import { Link } from 'react-router-dom'; import { ArrowLeft } from 'lucide-react'; import { Brand } from '../components/Brand.jsx'; import { api } from '../services/api.js'; import { Notice } from '../components/Toast.jsx'; import { Button } from '../components/Button.jsx';
export default function ForgotPassword() { const [email, setEmail] = useState(''); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const submit = async (e) => { e.preventDefault(); try { const data = await api('/auth/forgot-password', { method: 'POST', body: JSON.stringify({ email }) }); setMessage(data.message); } catch (x) { setError(x.message); } }; return <main className="auth-page"><Link to="/"><Brand /></Link><section className="auth-card"><h1>Reset your password</h1><p>Enter your registered email address. In production, a configured email provider sends the secure reset link.</p><Notice type="success">{message}</Notice><Notice>{error}</Notice><form onSubmit={submit}><label>Email address<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} /></label><Button>Request reset</Button></form><Link className="back-link" to="/login"><ArrowLeft size={16} /> Back to login</Link></section></main>; }
