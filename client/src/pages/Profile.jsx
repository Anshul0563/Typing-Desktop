import { useState } from 'react'; import { api } from '../services/api.js'; import { useAuth } from '../context/AuthContext.jsx'; import { Button } from '../components/Button.jsx'; import { Notice } from '../components/Toast.jsx';
export default function Profile() { const { user, setUser } = useAuth(); const [name, setName] = useState(user.name); const [passwords, setPasswords] = useState({ currentPassword: '', newPassword: '' }); const [notice, setNotice] = useState(''); const [error, setError] = useState(''); const saveProfile = async (e) => { e.preventDefault(); try { const d = await api('/auth/profile', { method: 'PATCH', body: JSON.stringify({ name }) }); setUser(d.user); setNotice('Profile updated.'); setError(''); } catch (x) { setError(x.message); } }; const change = async (e) => { e.preventDefault(); try { const d = await api('/auth/change-password', { method: 'PATCH', body: JSON.stringify(passwords) }); setNotice(d.message); setPasswords({ currentPassword: '', newPassword: '' }); setError(''); } catch (x) { setError(x.message); } }; return <><div className="page-heading"><div><h1>Your profile</h1><p>Manage your account details and password.</p></div></div><Notice type="success">{notice}</Notice><Notice>{error}</Notice><div className="settings-grid"><form className="panel form-panel" onSubmit={saveProfile}><h2>Personal information</h2><label>Full name<input value={name} onChange={(e) => setName(e.target.value)} required /></label><label>Email address<input value={user.email} disabled /></label><Button>Save changes</Button></form><form className="panel form-panel" onSubmit={change}><h2>Change password</h2><label>Current password<input type="password" required value={passwords.currentPassword} onChange={(e) => setPasswords({ ...passwords, currentPassword: e.target.value })} /></label><label>New password<input type="password" minLength="8" required value={passwords.newPassword} onChange={(e) => setPasswords({ ...passwords, newPassword: e.target.value })} /></label><Button>Update password</Button></form></div></>; }
